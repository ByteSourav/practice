#include <stdio.h>
#include <stdbool.h>  // Import the boolean header file 

int main() {
  bool this is testy= false;
  bool this is yeammy= true;// Returns 0 (false)
  printf("%d\n",testy);
  printf("%d",yeammy);
  return 0;
}
